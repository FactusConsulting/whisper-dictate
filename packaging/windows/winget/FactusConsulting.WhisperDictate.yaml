# Template: the release pipeline fills __VERSION__ and ships the concrete
# manifests as the whisper-dictate-winget-<version>.zip release asset.
PackageIdentifier: FactusConsulting.WhisperDictate
PackageVersion: __VERSION__
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.12.0
