{
  "_comment": "SINGLE SOURCE OF TRUTH for whisper-dictate runtime settings: VOICEPI_* env var <-> config.json key <-> default value <-> live-reloadable flag. nullable=true means config.json may store null as an explicit clear that suppresses environment fallback. Embedded by the Rust controller. Description/advanced/category feed the docs generator; the native setup wizard also consumes choices and numeric bounds. Regenerate docs after editing: pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/dev/gen-settings-docs.ps1",
  "settings": [
    {
      "env": "VOICEPI_KEY",
      "key": "key",
      "default": "pause",
      "live": false,
      "description": "Hold-to-talk hotkey; pause is the default. Settings reports syntax, the session's preflight driver, the actual installed listener, and session-only focused/unfocused verification where focus attribution is available. Windows uses RegisterHotKey for expressible chords and flags rdev fallback as a focus risk; X11 uses rdev and Wayland uses evdev. Run the guided test on Windows/X11 before relying on a chord; Wayland leaves it disabled because focus ownership is unavailable. Navigation, media, lock, and f13+ names are not supported by every native listener; letter/digit triggers remain outside the cross-platform UI vocabulary.",
      "advanced": false,
      "category": "core"
    },
    {
      "env": "VOICEPI_MODEL",
      "key": "model",
      "default": "large-v3-turbo",
      "live": false,
      "choices": ["large-v3-turbo", "large-v3"],
      "description": "Local Whisper model offered in Settings. Download the selected model explicitly before starting. Hidden legacy tiny, base, small, medium, tiny.en, base.en, and small.en values remain loadable so existing configurations and cached models continue to work.",
      "advanced": false,
      "category": "core"
    },
    {
      "env": "VOICEPI_STT_BACKEND",
      "key": "stt_backend",
      "default": "whisper",
      "live": false,
      "choices": ["whisper", "openai"],
      "description": "Speech-to-text engine: whisper (local native whisper.cpp) or openai (external OpenAI-compatible cloud API).",
      "advanced": false,
      "category": "core"
    },
    {
      "env": "VOICEPI_STT_MODEL",
      "key": "stt_model",
      "default": null,
      "live": false,
      "nullable": true,
      "description": "External transcription model used only when stt_backend=openai, e.g. gpt-4o-mini-transcribe, gpt-4o-transcribe, whisper-1, or a compatible name.",
      "advanced": true,
      "category": "stt-cloud"
    },
    {
      "env": "VOICEPI_STT_BASE_URL",
      "key": "stt_base_url",
      "default": "https://api.openai.com/v1",
      "live": false,
      "description": "OpenAI-compatible transcription API base URL, used only when stt_backend=openai (e.g. https://api.groq.com/openai/v1 for Groq).",
      "advanced": true,
      "category": "stt-cloud"
    },
    {
      "env": "VOICEPI_STT_TIMEOUT_MS",
      "key": "stt_timeout_ms",
      "default": "30000",
      "live": false,
      "min": 100,
      "max": 600000,
      "step": 100,
      "description": "Maximum wait (ms) for an external transcription request before it is abandoned.",
      "advanced": true,
      "category": "stt-cloud"
    },
    {
      "env": "VOICEPI_DEVICE",
      "key": "device",
      "default": "auto",
      "live": false,
      "choices": ["auto", "vulkan", "cpu"],
      "description": "Compute device for native local STT: auto uses the compiled GPU backend when available; vulkan explicitly requests the Vulkan backend; cpu disables GPU use.",
      "advanced": false,
      "category": "core"
    },
    {
      "env": "VOICEPI_AUDIO_DEVICE",
      "key": "audio_device",
      "default": null,
      "live": false,
      "nullable": true,
      "description": "Microphone/capture device: empty = OS default, an integer device index, or a case-insensitive name substring (e.g. Yeti). Backend-independent.",
      "advanced": false,
      "category": "core"
    },
    {
      "env": "VOICEPI_LANG",
      "key": "lang",
      "default": null,
      "live": true,
      "nullable": true,
      "description": "Spoken-language hint as an ISO 639-1 code (da, en, de, ...). Empty = auto-detect. Strongly recommended for Whisper.",
      "advanced": false,
      "category": "core"
    },
    {
      "env": "VOICEPI_XKB_LAYOUT",
      "key": "xkb_layout",
      "default": null,
      "live": false,
      "nullable": true,
      "description": "Wayland only: force the keycode layout for special-character injection (dk, se, de, fi, no, ...), overriding auto-detection.",
      "advanced": true,
      "category": "injection"
    },
    {
      "env": "VOICEPI_INITIAL_PROMPT",
      "key": "initial_prompt",
      "default": null,
      "live": true,
      "nullable": true,
      "description": "Free-text vocabulary/context hint (up to ~1024 chars) biasing recognition toward your domain words and names.",
      "advanced": true,
      "category": "stt-local"
    },
    {
      "env": "VOICEPI_INJECT_MODE",
      "key": "inject_mode",
      "default": "auto",
      "live": true,
      "choices": ["auto", "type", "paste", "print"],
      "description": "Text output strategy: auto (type, paste on fragile Windows terminals), type (direct keystrokes), paste (clipboard + paste on X11/Windows), or print (stdout only).",
      "advanced": false,
      "category": "core"
    },
    {
      "env": "VOICEPI_FORMAT_COMMANDS",
      "key": "format_commands",
      "default": "off",
      "live": true,
      "choices": ["off", "en", "da", "both"],
      "description": "Opt-in deterministic spoken formatting commands: off, en (new line/comma/period), da (ny linje/komma/punktum), or both.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_MAX_CHARS_PER_SECOND",
      "key": "max_chars_per_second",
      "default": "30",
      "live": true,
      "min": 0,
      "max": 100,
      "step": 1,
      "description": "Speech-rate plausibility gate: drop a transcript whose chars/second exceeds this (0 disables). Real speech is ~15-25 chars/s; impossible rates flag a hallucination.",
      "advanced": true,
      "category": "stt-local"
    },
    {
      "env": "VOICEPI_MIN_RECORD_SECONDS",
      "key": "min_record_seconds",
      "default": "0.5",
      "live": true,
      "min": 0,
      "max": 5,
      "step": 0.1,
      "description": "Discard recordings shorter than this as accidental key taps (effective floor max(0.3, value)), avoiding hallucinated credits on quiet sub-second taps.",
      "advanced": true,
      "category": "stt-local"
    },
    {
      "env": "VOICEPI_RELEASE_TAIL_MS",
      "key": "release_tail_ms",
      "default": "200",
      "live": true,
      "min": 0,
      "max": 2000,
      "step": 50,
      "description": "Keep capturing briefly (ms; 0 disables) after the hotkey is released so final syllables/words are not clipped.",
      "advanced": true,
      "category": "audio"
    },
    {
      "env": "VOICEPI_PREVIEW_SECONDS",
      "key": "preview_seconds",
      "default": "3",
      "live": false,
      "min": 0,
      "max": 30,
      "step": 1,
      "description": "Local Whisper only: re-transcribe the buffer this often (seconds; 0 disables) so the live Runtime card shows the sentence growing. Display-only.",
      "advanced": true,
      "category": "stt-local"
    },
    {
      "env": "VOICEPI_MAX_RECORD_S",
      "key": "max_record_s",
      "default": "120",
      "live": true,
      "min": 0,
      "max": 600,
      "step": 5,
      "description": "Maximum recording length (seconds; 0 disables the cap). Beyond it, further audio is dropped with a warning; audio up to the cap is still transcribed.",
      "advanced": true,
      "category": "audio"
    },
    {
      "env": "VOICEPI_TARGET_DBFS",
      "key": "target_dbfs",
      "default": "-20",
      "live": true,
      "description": "Loudness target (dBFS, <= 0) for quiet-boost normalisation. Lower (e.g. -16) boosts quiet speech harder.",
      "advanced": true,
      "category": "audio"
    },
    {
      "env": "VOICEPI_MIN_INPUT_DBFS",
      "key": "min_input_dbfs",
      "default": "-55",
      "live": true,
      "description": "Reject utterances quieter than this (dBFS) as 'input too quiet'.",
      "advanced": true,
      "category": "audio"
    },
    {
      "env": "VOICEPI_MIN_SNR_DB",
      "key": "min_snr_db",
      "default": "6",
      "live": true,
      "description": "Reject utterances with speech-vs-noise contrast below this (dB) as 'no speech contrast'.",
      "advanced": true,
      "category": "audio"
    },
    {
      "env": "VOICEPI_AUDIO_DUCKING",
      "key": "audio_ducking",
      "default": null,
      "live": false,
      "description": "Windows only: while recording, lower other apps' audio sessions and restore them before transcription. Disabled by default.",
      "advanced": true,
      "category": "audio"
    },
    {
      "env": "VOICEPI_AUDIO_DUCKING_LEVEL",
      "key": "audio_ducking_level",
      "default": "0.25",
      "live": false,
      "min": 0.0,
      "max": 1.0,
      "step": 0.05,
      "description": "Target volume (0.0-1.0) for other apps' audio while recording when audio ducking is enabled.",
      "advanced": true,
      "category": "audio"
    },
    {
      "env": "VOICEPI_DICTIONARY",
      "key": "dictionary",
      "default": null,
      "live": true,
      "nullable": true,
      "description": "Path(s) to custom dictionary file(s) providing a bounded vocabulary prompt plus exact smart replacements for product/app names.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_DICTIONARY_ENABLED",
      "key": "dictionary_enabled",
      "default": "1",
      "live": true,
      "description": "Toggle dictionary loading without removing the file. Set 0/false/no/off to disable.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_DICTIONARY_MAX_TERMS",
      "key": "dictionary_max_terms",
      "default": "80",
      "live": true,
      "min": 0,
      "max": 1000,
      "step": 1,
      "description": "Maximum number of dictionary terms appended to the Whisper prompt, keeping prompt injection bounded as the dictionary grows.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_DICTIONARY_PROMPT_CHARS",
      "key": "dictionary_prompt_chars",
      "default": "1200",
      "live": true,
      "min": 0,
      "max": 20000,
      "step": 100,
      "description": "Maximum total characters used by dictionary terms in the prompt.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_JSON",
      "key": "json_output",
      "default": null,
      "live": false,
      "description": "Print one structured JSON event per accepted utterance to stdout; also gates the metrics file (metrics_jsonl is only written while this is on).",
      "advanced": true,
      "category": "diagnostics"
    },
    {
      "env": "VOICEPI_METRICS_JSONL",
      "key": "metrics_jsonl",
      "default": null,
      "live": false,
      "nullable": true,
      "description": "Append one structured JSON event per accepted utterance to this file. Only written while JSON output is enabled.",
      "advanced": true,
      "category": "diagnostics"
    },
    {
      "env": "VOICEPI_COMMAND_HOOK",
      "key": "command_hook",
      "default": null,
      "live": true,
      "nullable": true,
      "description": "Advanced opt-in automation hook (command string or JSON array). Receives one utterance JSON event on stdin and runs without shell interpolation.",
      "advanced": true,
      "category": "diagnostics"
    },
    {
      "env": "VOICEPI_COMMAND_HOOK_TIMEOUT_MS",
      "key": "command_hook_timeout_ms",
      "default": "2000",
      "live": true,
      "min": 100,
      "max": 600000,
      "step": 100,
      "description": "Maximum wait (ms) for the command hook. Timeout/failure is logged and recorded but does not block injection.",
      "advanced": true,
      "category": "diagnostics"
    },
    {
      "env": "VOICEPI_HISTORY_ENABLED",
      "key": "history_enabled",
      "default": "1",
      "live": false,
      "description": "Store accepted live dictations locally for copy/reinject/debug recovery. Set 0/false/no/off to disable.",
      "advanced": true,
      "category": "diagnostics"
    },
    {
      "env": "VOICEPI_HISTORY_JSONL",
      "key": "history_jsonl",
      "default": null,
      "live": false,
      "nullable": true,
      "description": "Override the local history JSONL path (default under the per-user state dir).",
      "advanced": true,
      "category": "diagnostics"
    },
    {
      "env": "VOICEPI_LOCAL_ONLY",
      "key": "local_only",
      "default": null,
      "live": false,
      "description": "Privacy lock: block cloud/BYOK backends and force model libraries into offline mode (HF/Transformers/W&B offline). A library/runtime guard, not an OS firewall rule.",
      "advanced": true,
      "category": "stt-cloud"
    },
    {
      "env": "VOICEPI_POST_PROCESSOR",
      "key": "post_processor",
      "default": "none",
      "live": false,
      "choices": ["none", "ollama", "openai", "groq"],
      "description": "Optional second text pass after STT and dictionary replacements: none, ollama (local), or openai/groq (cloud, blocked by local_only).",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_POST_MODE",
      "key": "post_mode",
      "default": "raw",
      "live": true,
      "choices": ["raw", "clean", "prompt", "terminal", "slack", "email", "bullets"],
      "description": "Rewrite style for the post-processor: raw, clean, prompt, terminal, slack, email, or bullets (bullet-list alias).",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_POST_MODEL",
      "key": "post_model",
      "default": "qwen2.5:3b",
      "live": true,
      "description": "Text model used by the selected post-processor: an Ollama model name or an OpenAI-compatible chat model.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_POST_BASE_URL",
      "key": "post_base_url",
      "default": "http://localhost:11434",
      "live": false,
      "description": "Post-processing endpoint (local Ollama by default; an OpenAI-compatible /chat/completions URL for cloud). Blocked for external providers by local_only.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_POST_TIMEOUT_MS",
      "key": "post_timeout_ms",
      "default": "4000",
      "live": true,
      "min": 100,
      "max": 600000,
      "step": 100,
      "description": "Base/floor wall-clock budget (ms) for the rewrite; the effective timeout scales with length (+20 ms/char up to a 30 s ceiling) then falls back to dictionary-final text.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_POST_MAX_INPUT_CHARS",
      "key": "post_max_input_chars",
      "default": "4000",
      "live": true,
      "min": 100,
      "max": 100000,
      "step": 100,
      "description": "Maximum number of characters sent to the post-processor.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_POST_MAX_OUTPUT_CHARS",
      "key": "post_max_output_chars",
      "default": "4000",
      "live": true,
      "min": 100,
      "max": 100000,
      "step": 100,
      "description": "Maximum number of rewritten characters accepted back from the post-processor.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_POST_REDACT",
      "key": "post_redact",
      "default": null,
      "live": true,
      "description": "Opt-in local redaction before cloud post-processing: replace emails, phone numbers and common API tokens with placeholders, restored afterward when possible.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_POST_REDACT_TERMS",
      "key": "post_redact_terms",
      "default": null,
      "live": true,
      "nullable": true,
      "description": "Extra comma-separated names/terms to redact before cloud post-processing. Original values are never written to metrics.",
      "advanced": true,
      "category": "postprocess"
    },
    {
      "env": "VOICEPI_FEEDBACK_SOUNDS",
      "key": "feedback_sounds",
      "default": null,
      "live": true,
      "description": "Play a short audio cue on record start/stop, useful when the console is hidden (headless/autostart). Non-blocking.",
      "advanced": true,
      "category": "injection"
    },
    {
      "env": "VOICEPI_LOG",
      "key": "log_level",
      "default": "info",
      "live": true,
      "choices": ["off", "info", "debug", "trace"],
      "description": "Native diagnostic verbosity: off, info (lifecycle), debug (runtime decisions and action flow), or trace (high-volume input, environment-key, and teardown flow).",
      "advanced": true,
      "category": "diagnostics"
    },
    {
      "env": "VOICEPI_TOGGLE",
      "key": "toggle_mode",
      "default": null,
      "live": false,
      "description": "Toggle mode: press the hotkey to start recording, press again to stop and transcribe, instead of holding it. Restart-only.",
      "advanced": true,
      "category": "injection"
    },
    {
      "env": "VOICEPI_UPDATE_CHECK",
      "key": "update_check",
      "default": "1",
      "live": true,
      "description": "UI only: periodically check the public GitHub version list and show a discreet 'update available' badge. Sends no data/telemetry; skipped when local_only is on.",
      "advanced": true,
      "category": "updates"
    },
    {
      "env": "VOICEPI_UPDATE_CHECK_INTERVAL_MINUTES",
      "key": "update_check_interval_minutes",
      "default": "15",
      "live": true,
      "min": 5,
      "max": 1440,
      "step": 5,
      "description": "UI only: how often (minutes, clamped to >= 5) the in-app update check polls the public version list.",
      "advanced": true,
      "category": "updates"
    },
    {
      "env": "VOICEPI_UPDATE_INCLUDE_PRERELEASES",
      "key": "update_include_prereleases",
      "default": "0",
      "live": true,
      "description": "UI only: opt in to update notifications for release candidates (pre-releases), not just final releases. Off by default; live on the next poll.",
      "advanced": true,
      "category": "updates"
    }
  ]
}
